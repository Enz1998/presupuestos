const ConvertApi = require('convertapi');
const ca = ConvertApi('test');
// We can't easily mock the result without an API call
// But we can check the library source or prototype if needed
// Actually, I'll just look for common patterns online.
